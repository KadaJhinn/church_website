name: church_wireframe_dashboard
description: A Flutter web dashboard project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  fl_chart: ^0.66.0
  image_picker: ^1.0.7
  table_calendar: ^3.0.9

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.0
  http: ^1.6.0
  camera: ^0.11.2+1
  image: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true